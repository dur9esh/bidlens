# Required: get from https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional: set if you want to override the auto-detected base URL when calling /api/health from the client during local dev
# NEXT_PUBLIC_BASE_URL=http://localhost:3000
